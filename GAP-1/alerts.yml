groups:
  - name: cms_alerts
    interval: 10s
    rules:
      - alert: CMSDown
        expr: probe_success{job="blackbox"} == 0
        for: 30s
        labels:
          severity: critical
        annotations:
          summary: "CMS is down"
          description: "WordPress is not responding on {{ $labels.instance }}"